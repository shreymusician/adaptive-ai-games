{
  "name": "@adaptive-ai/pattern-recognition",
  "version": "0.0.0",
  "private": true,
  "description": "Adaptive AI Engine — Pattern Recognition: discovers discrete, statistically-backed behavioral habits from match history. Answers only \"what habits does this player repeatedly demonstrate\" — never planning or decision-making.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root .",
    "test:watch": "vitest --root ."
  },
  "dependencies": {
    "@adaptive-ai/memory-engine": "*",
    "@adaptive-ai/player-modeling": "*",
    "mongodb": "^6.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
