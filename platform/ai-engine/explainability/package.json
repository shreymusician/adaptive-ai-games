{
  "name": "@adaptive-ai/explainability",
  "version": "0.0.0",
  "private": true,
  "description": "Adaptive AI Engine — Explainability Engine: converts the Decision Engine's and Strategy Planner's already-stored reasoning traces into deterministic, evidence-backed, awareness-budget-truthful explanations. Never a post-hoc generative step — a template-filled readout of real trace data only.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root .",
    "test:watch": "vitest --root ."
  },
  "dependencies": {
    "@adaptive-ai/sdk-protocol": "*",
    "@adaptive-ai/strategy-planner": "*",
    "@adaptive-ai/decision-engine": "*",
    "@adaptive-ai/memory-engine": "*",
    "mongodb": "^6.0.0",
    "express": "^4.18.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "@types/supertest": "^6.0.0",
    "supertest": "^6.3.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
