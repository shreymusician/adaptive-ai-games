{
  "name": "@adaptive-ai/sdk-protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and postMessage contract between sdk-client and sdk-host — types only, no runtime coupling between the two trust domains",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ."
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
