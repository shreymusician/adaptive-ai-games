{
  "name": "@adaptive-ai/event-pipeline",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical event ingestion, batching, validation, and persistence",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ."
  }
}
