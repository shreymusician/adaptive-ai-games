{
  "name": "@adaptive-ai/event-pipeline",
  "version": "0.0.0",
  "private": true,
  "description": "Event ingestion, validation, persistence, and replay for the Adaptive AI Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root .",
    "test:watch": "vitest --root ."
  },
  "dependencies": {
    "@adaptive-ai/sdk-protocol": "*",
    "mongodb": "^6.0.0",
    "express": "^4.18.0",
    "zod": "^3.22.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.10"
  }
}
